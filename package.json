{
  "name": "beez-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.tsx",
  "scripts": {
    "install-server": "cd backend && npm install",
    "start-server": "cd backend && npm run start",
    "watch-server": "cd backend && npm run watch",
    "install-client": "cd client && npm install",
    "build-client": "cd client && npm run build",
    "start-client": "cd client && npm run start",
    "watch-client": "cd client && npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^20.11.5",
    "install": "^0.13.0",
    "npm": "^10.3.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "prettier": "^3.2.4",
    "tsc-watch": "^6.0.4",
    "typescript": "^5.3.3"
  }
}
